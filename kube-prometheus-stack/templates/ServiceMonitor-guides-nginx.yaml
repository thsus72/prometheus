apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: guides-service-nginx-servicemonitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: guides-service-nginx
  namespaceSelector:
    matchNames:
      - guides-service
  endpoints:
    - port: http
      path: /nginx_status
      interval: 30s
