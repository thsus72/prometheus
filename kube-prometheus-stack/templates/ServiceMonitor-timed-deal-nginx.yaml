apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: timed-deal-service-nginx-servicemonitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: timed-deal-service-nginx
  namespaceSelector:
    matchNames:
      - timed-deal-service
  endpoints:
    - port: http
      path: /nginx_status
      interval: 30s
