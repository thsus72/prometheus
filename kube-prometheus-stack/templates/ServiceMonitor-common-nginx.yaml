apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: common-service-nginx-servicemonitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: common-service-nginx
  namespaceSelector:
    matchNames:
      - common-service
  endpoints:
    - port: http
      path: /nginx_status
      interval: 30s
